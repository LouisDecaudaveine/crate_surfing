export type Track = {
    id: string;
    artist: string;
    track_title: string;
    album: string;
    bpm: number;
    music_key: string;
    file_id: string;
    tags: string;
    date_added: Date;
}
// track title, artist, album, tags, bpm, key, dated added